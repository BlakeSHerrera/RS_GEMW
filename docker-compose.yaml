services:
  extract:
    build: .
    command: python extract/main.py
    env_file: .env
    environment:
      - PYTHONUNBUFFERED=1
    stdin_open: true
    tty: true
    volumes:
      - rs_gemw_data:/app/data

volumes:
  rs_gemw_data:
